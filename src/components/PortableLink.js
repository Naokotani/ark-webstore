import React from 'react';

const PortableLink = (props) => {
	return (
		<span></span>
	)
}

export default PortableLink;

			// {props.mark.replace ? (
			// <Link to={props.mark.to} replace>
			//  	{props.children}
			//  </Link>
			// ) : (
			// <Link to={props.mark.to}>
			//  	{props.children}
			//  </Link>
			// )}
